export interface Project {
  id: string;
  clientName: string;
  projectName: string;
  sourceLanguage: string;
  targetLanguage: string;
  translatorName: string;
  status: string;
  imageUrl: string;
}